# System SDK

Rules systems declare:

- `system.manifest.json`
- actor JSON schema
- item JSON schema
- sheet registrations
- dice formula registrations
- optional server hooks

See `plugins/example-system-generic-fantasy` for a minimal installable system.
