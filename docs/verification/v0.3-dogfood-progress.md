# v0.3 Outside Dogfood Progress

Date: 2026-05-12

## 2026-05-12 Dogfood Usability Slice

### Changed

- Added v0.3 readiness, progress, and acceptance tracking docs.
- Added outside dogfood guides for first-run GMs, players, invites/sessions, troubleshooting, issue reporting, known issues, triage labels, and the first-session checklist.
- Added a redacted `GET /api/v1/campaigns/:campaignId/dogfood-report-bundle` endpoint for reproducible issue reports without user emails, auth state, journal bodies, chat bodies, AI messages, AI tool payloads, raw content-import data, asset URLs, or asset bytes.
- Added focused API coverage for sanitized dogfood report bundles.
- Added focused SQLite restart and failed-import recovery coverage for beta archive state and content-import previews.
- Added SQLite persistence for `contentImports`.
- Added a web Content tab for safe user-provided content import preview/apply/rollback/delete.
- Added a sidebar Report Bundle export button.
- Bumped workspace package/runtime metadata to `0.3.0` while keeping campaign archive exports at schema `0.2.0`.
- Updated release and roadmap docs so beta v0.2 is treated as accepted and v0.3 is the current outside-dogfood track.

### Validation

- `pnpm --filter @open-tabletop/web typecheck` passed.
- `pnpm --filter @open-tabletop/api test -- -t "dogfood report bundle|sqlite restart"` passed: 2 selected tests, 115 skipped.
- `pnpm --filter @open-tabletop/api typecheck` passed.
- `pnpm check` passed lint, typecheck, tests, and production build.
- `git diff --check` passed with only Windows LF-to-CRLF working-copy warnings.
- Clean checkout verification passed from `C:\Users\treyg\AppData\Local\Temp\otte-v03-clean-20260511200334`:
  - `pnpm install --frozen-lockfile --dir C:\Users\treyg\AppData\Local\Temp\otte-v03-clean-20260511200334` completed with the lockfile unchanged.
  - `pnpm --dir C:\Users\treyg\AppData\Local\Temp\otte-v03-clean-20260511200334 check` passed lint, typecheck, tests, and production build.
- Live runtime smoke passed:
  - API dev server: `http://127.0.0.1:4400`
  - Web dev server: `http://localhost:5173`
  - `GET /api/v1/health` returned `version: "0.3.0"` and `service: "open-tabletop-api"`.
  - Web root returned HTTP 200 and title `OpenTabletop Engine`.
  - Session-token auth imported `docs/demo/ember-vault-beta-dogfood.ottx.json` as `camp_beta_ember_vault`.
  - Exported `camp_beta_ember_vault` as archive `0.2.0` with manifest schema `0.2.0`.
  - Fetched dogfood report bundle format `otte-dogfood-report-bundle`, version `0.3.0`, with 6 actors.
  - Live content import preview/apply/rollback/delete returned `previewed`, `applied`, `rolled_back`, and `deleted`.

### Final Status

- Accepted for v0.3 outside dogfood. Final acceptance is recorded in `docs/verification/v0.3-dogfood-acceptance.md`.
