# v0.3 Outside Dogfood Acceptance

Date: 2026-05-12

## Final Status

Accepted for v0.3 outside dogfood on 2026-05-12. The release has user-facing dogfood docs, a safe content-import workflow, a redacted report bundle path, focused regression coverage, live API/web smoke evidence, full workspace validation, and a clean-checkout verification pass.

## Prompt-To-Artifact Checklist

| Requirement | Evidence | Current result |
| --- | --- | --- |
| v0.3 verification docs exist | `docs/verification/v0.3-dogfood-readiness.md`, `docs/verification/v0.3-dogfood-progress.md`, this file | Ready. |
| README routes non-expert users | `README.md` v0.3 outside dogfood section | Ready. |
| Outside dogfood onboarding exists | `docs/dogfood/*.md` guides and checklist | Ready. |
| Campaign setup UX improved | Web sidebar import status, campaign switching, report bundle export, Content tab | Accepted. Runtime smoke imported the beta dogfood archive, exported it, fetched the report bundle, and loaded the web root. |
| Safe content import UX usable | Web Content tab plus API tests | Accepted. Focused API tests passed, and live preview/apply/rollback/delete returned `previewed`, `applied`, `rolled_back`, and `deleted`. |
| Report issue loop exists | Issue template, issue-reporting doc, known issues, triage labels, redacted report bundle endpoint | Focused report bundle test passed. |
| Reliability for restart/failed import | SQLite restart and failed-import recovery API test | Focused test passed. |
| Beta archive upgrades still work | Existing alpha/beta archive tests plus final `pnpm check` | Accepted. Full workspace checks passed locally and from a clean checkout. |
| AI dogfood quality | Existing beta eval test and v0.3 acceptance run | Accepted through the full API regression suite in `pnpm check`, including beta dogfood coverage. |
| Plugin/system smoke | Existing beta plugin/system regression and v0.3 acceptance run | Accepted through the full API/plugin/system regression suites in `pnpm check`. |
| API/web runtime | Local API/web start and health/web smoke | Accepted. API ran on `http://127.0.0.1:4400`; web ran on `http://localhost:5173`; health returned version `0.3.0`; web root returned HTTP 200 and title `OpenTabletop Engine`. |
| 1 GM + 3 players and 3-session runbook | Beta runbook plus GM+3 evidence | Accepted through existing beta runbook evidence and the GM+3 realtime API regression included in full `pnpm check`. |
| Export/import after sessions | Archive export/import proof after runbook | Accepted. Runtime smoke imported `docs/demo/ember-vault-beta-dogfood.ottx.json` and exported `camp_beta_ember_vault` as archive `0.2.0` with manifest schema `0.2.0`. |
| `pnpm check` passes | Command output | Accepted locally and from a clean checkout. |

## Current Commands

- Passed: `pnpm --filter @open-tabletop/web typecheck`
- Passed: `pnpm --filter @open-tabletop/api test -- -t "dogfood report bundle|sqlite restart"`
- Passed: `pnpm --filter @open-tabletop/api typecheck`
- Passed: `pnpm check`
- Passed: `git diff --check`
- Passed: `pnpm install --frozen-lockfile --dir C:\Users\treyg\AppData\Local\Temp\otte-v03-clean-20260511200334`
- Passed: `pnpm --dir C:\Users\treyg\AppData\Local\Temp\otte-v03-clean-20260511200334 check`

## Runtime Evidence

- API dev server: `http://127.0.0.1:4400`
- Web dev server: `http://localhost:5173`
- `GET /api/v1/health` returned `version: "0.3.0"` and `service: "open-tabletop-api"`.
- Web root returned HTTP 200 and title `OpenTabletop Engine`.
- Imported `docs/demo/ember-vault-beta-dogfood.ottx.json` as `camp_beta_ember_vault`.
- Exported `camp_beta_ember_vault` as archive `0.2.0` with manifest schema `0.2.0`.
- Fetched dogfood report bundle format `otte-dogfood-report-bundle`, version `0.3.0`, with 6 actors.
- Live content import preview/apply/rollback/delete returned `previewed`, `applied`, `rolled_back`, and `deleted`.

## Acceptance Rule

Satisfied. Concrete command output and runtime evidence are recorded above.
